name: Node.js CI MasterDeployCI

on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: Install Packages
      run: npm install
    - name: Deploy to GH Pages
      run: |
        git config --global user.email "ela.ri.bag@gmail.com"
        git config --global user.name "GabrielaSaldana"
        git remote set-url origin https://6e36d374b3317217de3b272d9835890213d9cb09@github.com/GabrielaSaldana/profile.git
        npm run deploy
    - name: Updating Domain
      run: echo "Update domain"
      env:
        CI: true
